/* eslint-disable arrow-body-style */

// export const initialMailboxes = () => {
//   return Object.prototype.hasOwnProperty.call(window, 'mailmole')
//     && Object.prototype.hasOwnProperty.call(window.mailmole, 'mailboxes')
//     ? window.mailmole.mailboxes
//     : [];
// };
export const initialMailboxes = () => {
  return [];
};

export const user = null;
